// Used as Entry Point for VS Code Debugger ** only **
// Use ./src/app.js for normal initializations

// babel-register is currently set as devDependency because it
// is only used for the VS code debugger. Adjust this if need be

require('babel-register');
require('./src/app');